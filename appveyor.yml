# master
-
  branches:
    only:
      - master 

  version: 1.1.0

  skip_tags: true

  clone_depth: 1

  build:
    parallel: true
    publish_nuget: true
    verbosity: quiet

  os: Visual Studio 2017

  build_script:
    - ps: $env:BUILD_VERSION = $env:appveyor_build_version
    - ps: .\build.ps1

  on_failure:
    - ps: Update-AppveyorBuild -Version "$($env:appveyor_build_version)-$(Get-Date -format HHmmss)-failed"    
    - ps: $wc = New-Object 'System.Net.WebClient'
    - ps: $wc.UploadFile("https://ci.appveyor.com/api/testresults/xunit/$($env:APPVEYOR_JOB_ID)", (Resolve-Path .\TestResult.xml))
    

  on_success:
    - ps: $wc = New-Object 'System.Net.WebClient'
    - ps: $wc.UploadFile("https://ci.appveyor.com/api/testresults/xunit/$($env:APPVEYOR_JOB_ID)", (Resolve-Path .\TestResult.xml))
    - ps: git config --global user.email "$($env:Git_Email)"
    - ps: git config --global user.name "$($env:Git_Username)"
    - ps: git config --global credential.helper store
    - ps: Add-Content "$env:USERPROFILE\.git-credentials" "https://$($env:Git_Username):$($env:Git_Password)@bitbucket.org`n"
    - ps: echo $($env:APPVEYOR_REPO_NAME)
    - ps: git remote add bitbucket https://$($env:Git_Username)@github.com/$($env:APPVEYOR_REPO_NAME).git
    - ps: git tag $($env:appveyor_build_version) $($env:APPVEYOR_REPO_COMMIT)
    - ps: git push bitbucket --tags --quiet 

-
  branches:
    except:
      - master

  version: 1.1.0.{build}-{branch}

  skip_tags: true

  clone_depth: 1

  build_script:
    - ps: $env:BUILD_VERSION = $env:appveyor_build_version
    - ps: .\build.ps1

  on_success:
    - ps: $wc = New-Object 'System.Net.WebClient'
    - ps: $wc.UploadFile("https://ci.appveyor.com/api/testresults/xunit/$($env:APPVEYOR_JOB_ID)", (Resolve-Path .\TestResult.xml))
    - ps: git config --global user.email "$($env:Git_Email)"
    - ps: git config --global user.name "$($env:Git_Username)"
    - ps: git config --global credential.helper store
    - ps: Add-Content "$env:USERPROFILE\.git-credentials" "https://$($env:Git_Username):$($env:Git_Password)@bitbucket.org`n"
    - ps: echo $($env:APPVEYOR_REPO_NAME)
    - ps: git remote add bitbucket https://$($env:Git_Username)@github.com/$($env:APPVEYOR_REPO_NAME).git
    - ps: git tag $($env:appveyor_build_version) $($env:APPVEYOR_REPO_COMMIT)
    - ps: git push bitbucket --tags --quiet 

  on_failure:
    - ps: Update-AppveyorBuild -Version "$($env:appveyor_build_version)-$(Get-Date -format HHmmss)-failed"    
    - ps: $wc = New-Object 'System.Net.WebClient'
    - ps: $wc.UploadFile("https://ci.appveyor.com/api/testresults/xunit/$($env:APPVEYOR_JOB_ID)", (Resolve-Path .\TestResult.xml))